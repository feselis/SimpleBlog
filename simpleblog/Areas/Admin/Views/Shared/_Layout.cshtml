@using System.Web.Optimization;

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <title>SimpleBlog Admin</title>

    @Styles.Render("~/admin/styles")
</head>
<body>

    <!--Avaliar qual a diferença prática entre usar o ActionLink e o RouteLink-->
    <div class="container">
        <ul class="nav nav-pills">
            <li>
                @Html.ActionLink("Posts", "Index", "Posts", new {area = "admin" }, new {} )
            </li>
            <li>
                @Html.ActionLink("Users", "Index", "Users", new { area = "admin"}, new { })
            </li>
            <li class="pull-right">
                @Html.RouteLink("Voltar para o website", "home");
            </li>
        </ul>
    </div>


    
    <h1>Admin Area</h1>

    @RenderBody()

    @RenderSection("Scripts", false)

</body>
</html>
